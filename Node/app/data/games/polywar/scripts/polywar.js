class PolyWarGame extends Game {
  constructor(name) {
    super(name);
  }

  register(keys) {
  }

  unregister(keys) {
  }

  update(tickPart) {
  }

  //ctx = canvas 2d drawing context
  render(ctx, width, height) {
  }
}