//creates and stores a "network": a bunch of lines emanating from a single point in a grid-based pattern
class Network {
  //generates a network with n nodes
  //w, h: horizontal and vertical with of the grid, in # of nodes
  constructor(n, w, h) {
    
  }

  //will randomly create or remove nodes/edges to "animate" the network
  //can only remove nodes that have either 1, 3, or 4 connections
  //if there are no nodes at all, it may create one at a random location
  //can only add to nodes that have 1 connection (can add 1-3 more simultaneously)
  update() {
  }
};